server {
    listen 80;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;

    # Gzip compression
    gzip on;
    gzip_vary on;
    gzip_min_length 1024;
    gzip_types text/plain text/css text/xml text/javascript application/javascript application/xml+rss application/json image/svg+xml;

    # Cache de assets versionados (Astro añade hash en filenames)
    location /_astro/ {
        expires 1y;
        add_header Cache-Control "public, immutable";
    }

    # Imágenes y fuentes con caché
    location ~* \.(jpg|jpeg|png|gif|ico|svg|woff|woff2|ttf|eot)$ {
        expires 30d;
        add_header Cache-Control "public";
    }

    # SEO files
    location = /robots.txt {
        access_log off;
    }

    location = /favicon.ico {
        access_log off;
        log_not_found off;
    }

    # Astro genera /pagina/index.html para cada ruta — try_files maneja /pagina como /pagina/index.html
    location / {
        try_files $uri $uri/ $uri.html $uri/index.html =404;
    }

    # No cachear feed RSS (puede actualizarse con nuevos episodios)
    location = /podcast/feed.xml {
        expires off;
        add_header Cache-Control "public, max-age=300";
    }

    # Página de error 404 personalizada (si existe, si no Astro la sirve por la ruta)
    error_page 404 /404.html;
}
